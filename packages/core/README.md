# @kb-labs/core-workspace

Shared helpers for locating the kb-labs workspace umbrella directory. This package is intended for internal tooling consumption (CLI, REST, Studio) and is not published independently.

